use minimax_game::{game::GamePlayer, tic_tac_toe::BoardState};
fn main() {
    let board = BoardState::new();
    //let mut new_game = GamePlayer::from(board, (), RandomStrategy);
    //new_game.play();
}
